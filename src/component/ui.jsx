export const UserInterface=(props)=>{

    return(
        <div>
           <ul>
            <li>{`${props.elem.name}    ---Length  ${props.elem.name.length}`} </li>   
           </ul>
        </div>
    )


}